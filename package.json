{
  "name": "govorun",
  "version": "1.0.0",
  "description": "Govorun is tha speak editing model that allow you edit any files using only your vouce.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test:bdd:run": "cucumber-js --require dist/features/support/**/*.js",
    "test:jest:run": "jest --config jest.config.js",
    "test:bdd": "npm run build && npm run test:bdd:run",
    "test:jest": "npm run build && npm run test:jest:run",
    "test": "npm run build && npm run test:bdd:run && npm run test:jest:run"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@cucumber/cucumber": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
}
